import React from "react";
import ToggleButton from "../components/ToggleButton"; // Import the ToggleButton component

const App: React.FC = () => {
  return (
    <div>
      <h1>Toggle Button Example</h1>
      <ToggleButton /> {/* Render the ToggleButton */}
    </div>
  );
};

export default App;
